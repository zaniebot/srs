# linker-layout

This crate provides data structures for representing and persisting to disk, the layout used by a
linker such as sld. This layout information can then be used by tools such as linker-diff.
